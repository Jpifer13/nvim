-- ssh_remote is a local Lua module (lua/ssh_remote/), not an external plugin.
-- It has no additional dependencies beyond telescope.nvim which is already loaded.
-- This file exists only to keep the plugins/ directory self-documenting.
return {}
